# Leave unset or empty to call `/api/...` on the dev server (Vite proxies to http://localhost:8080).
# Set a full URL if the API is on another host, e.g. VITE_API_BASE_URL=http://localhost:8080
# VITE_API_BASE_URL=
